<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="db.properties"/>
    <typeAliases>
        <typeAlias type="com.gooodock.model.dto.MemberDTO" alias="MemberDTO"/>
        <typeAlias type="com.gooodock.model.dto.NotificationDTO" alias="NotificationDTO"/>
        <typeAlias type="com.gooodock.model.dto.SubscriptionDTO" alias="SubscriptionDTO"/>
    </typeAliases>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${user}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="mappers/MemberMapper.xml"/>
        <mapper resource="mappers/NotificationMapper.xml"/>
        <mapper resource="mappers/SubscriptionMapper.xml"/>
    </mappers>
</configuration>